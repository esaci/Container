#ifndef ITERATOR_OPERATORS_HPP
# define ITERATOR_OPERATORS_HPP
	# include <memory>
	# include <uchar.h>
	# include <cstddef>
		#include <iostream>
	/* template <class Category, class T,
			class Distance = std::ptrdiff_t, class Pointer = T*, class Reference = T&>
	class iterator;
	namespace ft{
		template<typename _IteratorL, typename _IteratorR, typename _Container>
		bool operator==(const iterator<_IteratorL, _Container>& __lhs,const iterator<_IteratorR, _Container>& __rhs)
		{ return __lhs.base() == __rhs.base(); }
	}; */

#endif